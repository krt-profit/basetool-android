# Spec Registry

Durable requirements for the Android app live here as docs-as-code, one file per area, using
ids `REQ-APP-<AREA>-NNN`. Every behaviour change updates its requirement in the same PR; a
change with no matching spec change is incomplete (see `CLAUDE.md`).

The first area has been extracted; for the rest the **concept documents in [`docs/`](../) remain
the binding source** (marked `Doc type: living plan`):

| Area (planned) | Will absorb |
|---|---|
| [`auth.md`](auth.md) (`REQ-APP-AUTH-*`) — **exists** (`001`–`013`) | login flow, token storage, DPoP, session states, app-lock, the refresh that keeps a session alive past one access token, the permission model and how a gated control refuses |
| [`api-contract.md`](api-contract.md) (`REQ-APP-API-*`) — **exists** (`001`–`006`) | consumed endpoints, headers, problem-code handling, pagination, version echo, and the rule that a response is handled off the main thread — a crash the JVM suite cannot see |
| [`settings.md`](settings.md) (`REQ-APP-SET-*`) — **exists** (`001`–`010`) | Einstellungen screen scope, per-app language, string-resource rule, legal links, the generated open-source notice, the screenshot switch, the sign-out confirmation |
| [`missions.md`](missions.md) (`REQ-APP-MIS-*`) — **exists** (`001`–`031`) | Einsatz list and detail: server-side filtering, "Vergangene aus" as a status filter, malformed-row tolerance, debouncing, paging, day grouping, the four list states; the detail's lazy Finanzen tab, the redacted answer, its three failure sentences and the required-enum fragility; and phase 3's own participation — sign-up, the check-in the server only allows once the Einsatz runs, and the payout preference — and the sheet that sign-up opens, because the share's destination and the desired function belong to the same moment; the manager half — the roster's three per-row actions gated on the server's own `canEdit`, the whole-row participant write the replace-semantics endpoint forces, and the Verwaltung tab that is drawn only for a manager — plus the two layout rules the owner's field report produced: one filled CTA with weights and no setting in the action bar, and no in-page control reusing a navigation label; and the write half of the remaining tabs — the Ablauf and Ziele counters the answer does not carry, the one debounced member picker behind three writes, and the two replace-shaped Einheit writes with their own locks |
| [`operations.md`](operations.md) (`REQ-APP-OPS-*`) — **exists** (`001`–`014`) | Operationen list and detail: the navigating segment, lazy loading behind it, the two approved design deviations (thin row, no income/expense split), running-versus-finished grouping, the three-reads-one-outcome detail, identity-by-id for "Dein Anteil", the capped/provisional caveats, and the one form that raises and rewrites an Operation |
| [`notifications.md`](notifications.md) (`REQ-APP-NOTIF-*`) — **exists** (`001`–`015`) | Inbox, bell badge and push: one state for both, poll plus SSE, the hand-rolled stream reader and its framing rules, device-side wording from type + named params with an honest fallback, why a row that leads nowhere is not clickable, the system shade, and the interactive half — optimistic actions, the delete that waits out its undo window rather than pretending to be reversible, and the swipe that never becomes the only path, the unread count's place in the top bar and the four-rung timestamp ladder shared with Bank and Dashboard |
| [`dashboard.md`](dashboard.md) (`REQ-APP-DASH-*`) — **exists** (`001`–`012`) | Übersicht: the design's reading order, two independently failing reads, the 204 that means "no banner", the server-clocked seven-day window, one source per greeting fact, the announcement's read marker, and the artboard details the first build read past — the greeting as a railed block, and quick-action glyphs that name the action rather than its destination |
| [`hangar.md`](hangar.md) (`REQ-APP-HANGAR-*`) — **exists** (`001`–`014`) | Hangar: my-ships over the permission-gated all-ships read, two halves with separate state, the flattened card, three distinct empty states, and why the design's three-number band cannot be computed |
| [`bank.md`](bank.md) (`REQ-APP-BANK-*`) — **exists** (`001`–`017`) | Bank: the member-facing paths over the bank-employee ones, account and ledger failing together, the sign taken from the booking kind, the balance line drawn rather than charted; phase 3's account settings, offered from the server's own `can*` flags rather than a role the app worked out; the booking requests, whose approval is one vote by a server-named class and never a count (ADR-0016); the staff surface, where confirming is a sheet because the server requires a Verwahrer; the staff dashboard's two shapes, whose absent totals must not be rendered as zeroes; and the account lifecycle, whose actions are drawn locked rather than hidden; and the grants matrix, whose three capability flags are joined by the entry itself as the view grant, which a removal therefore has to confirm; the holder detail, whose custody is the unit's and whose transfer costs the KRT account a fee; and the staff account detail, which reads through the office and whose Storno adds a booking rather than removing one |
| [`orders.md`](orders.md) (`REQ-APP-ORDERS-*`) — **exists** (`001`–`024`) | Aufträge: server-side status filter with no client scope, the redacted flag said out loud, the progress bar as a length rather than a figure, no invented "Zugesagt" total, the notification deep link; phase 3's writes — a member's own name on an order, the assignee note locked on its own version, and the status control the app asks about before offering; raising a material order, with its two differently-scoped unit pickers; the age counted in days; and moving an order in the queue without a drag; the item order behind the same form's kind switch, and an item order's own positions, which the app had been leaving unread; and the Übergabe — the write that finishes an Auftrag, booking a linked stock row out, with the delivered figure summed from the handover lines because the server's open remainder counts claims; and the Herstellung — the write that moves an item line's built figure, with a per-material consumption plan that has to cover the demand exactly and a mandatory book-in the artboard never drew; and the item Übergabe, whose ceiling is what has been BUILT rather than what was ordered, and whose log the app had been leaving unread; and the Zusagen, which belong to a Staffel rather than a member, exist only on a Spezialkommando order, and cannot overclaim however firmly the artboard says they can; and the edit — the create form pre-filled, writing one of two endpoints, with the requester's locked fields drawn rather than removed and a floor under every line at what has already changed hands; and the Materialsammelübersicht, where only a link with an amount behind it asks before it goes and the two fields the web edits inline are a stock transfer rather than a field edit; and the item positions, whose sub-assemblies are real ordered lines with a parent and whose edit is a Logistician's alone and only before the first handover |
| [`inventory.md`](inventory.md) (`REQ-APP-INV-*`) — **exists** (`001`–`020`) | Lager: three reads for three tree levels, a group that keeps what it loaded but not across a refresh, a failed group that stays open, a page-level stock filter that does not overstate itself; and phase 3's bookings — one sheet whose segment only offers what the entry can do, the version echo, offline as a disabled write, terminals read by the entry's own material, the note as its own mode, the selection mode and its batch report, a transfer that names all three of its targets with the org-unit pool preset to the one the entry is already in; the Sammel-Ausbuchen, which is whole rows and all-or-nothing because the endpoint carries only ids; and the Game-Item surface, whose rows open in place because the tree has no item mode to jump into |
| [`personal-inventory.md`](personal-inventory.md) (`REQ-APP-PI-*`) — **exists** (`001`–`015`) | Mein Inventar & Blueprints, phase 3's first writes: me-scoped by construction, the version echo and the conflict that keeps the typing, offline as a disabled write rather than a queued one, the editor's recorded deviation from the Lager form, a place picker that admits its cap, the multi-blueprint take-over whose batch call carries no note, and the org-wide blueprint availability — its own screen, its own role, owners loaded per row, and a „Nicht erfasst“ chip that admits it filters only what is loaded; and the selection mode whose bulk delete has to loop because no bulk endpoint exists |
| [`sync.md`](sync.md) (`REQ-APP-SYNC-*`) — **exists** (`001`–`005`) | Live sync: one SSE stream per screen with its rooms in the URL, the accepted list a screen has to read because silence and refusal look identical, ADR-0094's coalescing windows applied client-side, the app announcing its own writes and never a peer's, and the reconnect that keeps it an enhancement rather than a dependency |
| [`promotion.md`](promotion.md) (`REQ-APP-PROMO-*`) — **exists** (`001`–`003`) | Beförderung: the member's own assessments and rank standings, me-scoped by construction, two reads that fail independently, and the three judgements the screen refuses to make on the organisation's behalf — including the standing deviation that this area has no design chapter |
| [`materialboerse.md`](materialboerse.md) (`REQ-APP-MARKET-*`) — **exists** (`001`–`013`) | Materialbörse: a board that conveys interest and carries no place or handover, one model for two halves whose write paths stay apart, an item that is not a material and a unit that travels with the row, „Ich kann liefern" as a toggle that is never offered on your own entry, the supporter list as the server's answer rather than the app's inference, writes that update their row while only a create re-reads, a picked material that is an id and is dropped the moment the field is edited, the item half and the own-row editor that superseded the recorded gap — one sheet with a Material/Item switch, a product key instead of a stock row, and a withdrawal that asks only when somebody is waiting; two card columns on a tablet |
| [`materials.md`](materials.md) (`REQ-APP-MAT-*`) — **exists** (`001`–`004`) | Handel: the material catalogue held whole because its two price filters are not query parameters, the bound that drops the rows with no such price, the em dash that is never a zero, the category subtitle the price projection can actually serve; and one material's market, where the two head figures are a selection with their terminal named, and the table stays a table on the phone by shortening its headings rather than scrolling sideways. the matrix that is drawn as it arrives and whose one scroll state keeps every price under its own terminal; and the profit calculation, which is a SHIP calculation whose every figure is the server's and whose route sub-line the DTO cannot serve |
| [`refinery.md`](refinery.md) (`REQ-APP-REF-*`) — **exists** (`001`–`012`) | Raffinerie: the member's own orders only, „Abholbereit" derived from a clock the screen owns rather than read from a status the server does not have, the two live filters as one request split on the device and therefore showing no total, a booking derived entirely from the order and never sent empty, the three rooms one booking has to announce, the two recorded deviations from chapter 11 — no extractor import, and no invented UEX estimate; the Einlagern form, whose amount override is the reason it exists and which is one call because the server closes the order on it; the decimal comma the German keyboard produces and the app used to read as zero; the create form, whose completeness rules are the server's own and whose material fields must be pickers because the wire wants an id; and the edit and the deletion — the same form pre-filled from its own read, plus the two rules (a booked run's core is fixed, a booked run is not deleted) that the app enforces because the server does not |
| `privacy.md` (`REQ-APP-PRIV-*`) | dependency/data-flow gate, § 25 TDDDG storage table, permissions |
| [`ui.md`](ui.md) (`REQ-APP-UI-*`) — **exists** (`001`–`011`) | cross-screen behaviour: pull-to-refresh reaching an empty screen, a bottom sheet keeping its actions out of the gesture bar, the retry countdown of chapter 14 and the forced-update gate that stands outside every other gate and fails open three ways — including the rule only a screen can honour, that it replaces a screen with nothing on it and never one the member is already reading; and the tablet's list-detail, where the bar names the section and the pane names the row; the content gutter every screen shares, and the one end-of-list the design system draws. Still to come: KRT design tokens, adaptive layout rules, i18n + copy rules, Fan Kit compliance band (see `core/designsystem/fankit/`) — extracted from the binding design spec at `docs/design/android/` |
| `offline.md` (`REQ-APP-OFFLINE-*`) | read-cache scope, backup exclusion, wipe semantics |

Server-side requirements (API exposure, rate limits, monitoring) stay in the main `basetool`
repo's `docs/specs/` — never duplicated here.
