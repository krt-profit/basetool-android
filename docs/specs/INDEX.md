# Spec Registry

Durable requirements for the Android app live here as docs-as-code, one file per area, using
ids `REQ-APP-<AREA>-NNN`. Every behaviour change updates its requirement in the same PR; a
change with no matching spec change is incomplete (see `CLAUDE.md`).

The first area has been extracted; for the rest the **concept documents in [`docs/`](../) remain
the binding source** (marked `Doc type: living plan`):

| Area (planned) | Will absorb |
|---|---|
| [`auth.md`](auth.md) (`REQ-APP-AUTH-*`) — **exists** (`001`–`013`) | login flow, token storage, DPoP, session states, app-lock, the refresh that keeps a session alive past one access token, the permission model and how a gated control refuses |
| [`api-contract.md`](api-contract.md) (`REQ-APP-API-*`) — **exists** (`001`–`006`) | consumed endpoints, headers, problem-code handling, pagination, version echo, and the rule that a response is handled off the main thread — a crash the JVM suite cannot see |
| [`settings.md`](settings.md) (`REQ-APP-SET-*`) — **exists** (`001`–`010`) | Einstellungen screen scope, per-app language, string-resource rule, legal links, the generated open-source notice, the screenshot switch, the sign-out confirmation |
| [`missions.md`](missions.md) (`REQ-APP-MIS-*`) — **exists** (`001`–`018`) | Einsatz list and detail: server-side filtering, "Vergangene aus" as a status filter, malformed-row tolerance, debouncing, paging, day grouping, the four list states; the detail's lazy Finanzen tab, the redacted answer, its three failure sentences and the required-enum fragility; and phase 3's own participation — sign-up, the check-in the server only allows once the Einsatz runs, and the payout preference |
| [`operations.md`](operations.md) (`REQ-APP-OPS-*`) — **exists** (`001`–`013`) | Operationen list and detail: the navigating segment, lazy loading behind it, the two approved design deviations (thin row, no income/expense split), running-versus-finished grouping, the three-reads-one-outcome detail, identity-by-id for "Dein Anteil", and the capped/provisional caveats |
| [`notifications.md`](notifications.md) (`REQ-APP-NOTIF-*`) — **exists** (`001`–`013`) | Inbox, bell badge and push: one state for both, poll plus SSE, the hand-rolled stream reader and its framing rules, device-side wording from type + named params with an honest fallback, why a row that leads nowhere is not clickable, the system shade, and the interactive half — optimistic actions, the delete that waits out its undo window rather than pretending to be reversible, and the swipe that never becomes the only path, the unread count's place in the top bar and the four-rung timestamp ladder shared with Bank and Dashboard |
| [`dashboard.md`](dashboard.md) (`REQ-APP-DASH-*`) — **exists** (`001`–`008`) | Übersicht: the design's reading order, two independently failing reads, the 204 that means "no banner", the server-clocked seven-day window, and one source per greeting fact |
| [`hangar.md`](hangar.md) (`REQ-APP-HANGAR-*`) — **exists** (`001`–`009`) | Hangar: my-ships over the permission-gated all-ships read, two halves with separate state, the flattened card, three distinct empty states, and why the design's three-number band cannot be computed |
| [`bank.md`](bank.md) (`REQ-APP-BANK-*`) — **exists** (`001`–`009`) | Bank: the member-facing paths over the bank-employee ones, account and ledger failing together, the sign taken from the booking kind, the balance line drawn rather than charted; phase 3's account settings, offered from the server's own `can*` flags rather than a role the app worked out; the booking requests, whose approval is one vote by a server-named class and never a count (ADR-0016); and the staff surface, where confirming is a sheet because the server requires a Verwahrer |
| [`orders.md`](orders.md) (`REQ-APP-ORDERS-*`) — **exists** (`001`–`005`) | Aufträge: server-side status filter with no client scope, the redacted flag said out loud, the progress bar as a length rather than a figure, no invented "Zugesagt" total, the notification deep link; and phase 3's writes — a member's own name on an order, the assignee note locked on its own version, and the status control the app asks about before offering |
| [`inventory.md`](inventory.md) (`REQ-APP-INV-*`) — **exists** (`001`–`016`) | Lager: three reads for three tree levels, a group that keeps what it loaded but not across a refresh, a failed group that stays open, a page-level stock filter that does not overstate itself; and phase 3's bookings — one sheet whose segment only offers what the entry can do, the version echo, offline as a disabled write, terminals read by the entry's own material, the note as its own mode, the selection mode and its batch report, and a transfer that names all three of its targets with the org-unit pool preset to the one the entry is already in |
| [`personal-inventory.md`](personal-inventory.md) (`REQ-APP-PI-*`) — **exists** (`001`–`005`) | Mein Inventar & Blueprints, phase 3's first writes: me-scoped by construction, the version echo and the conflict that keeps the typing, offline as a disabled write rather than a queued one, the editor's recorded deviation from the Lager form, and a place picker that admits its cap |
| [`sync.md`](sync.md) (`REQ-APP-SYNC-*`) — **exists** (`001`–`005`) | Live sync: one SSE stream per screen with its rooms in the URL, the accepted list a screen has to read because silence and refusal look identical, ADR-0094's coalescing windows applied client-side, the app announcing its own writes and never a peer's, and the reconnect that keeps it an enhancement rather than a dependency |
| [`promotion.md`](promotion.md) (`REQ-APP-PROMO-*`) — **exists** (`001`–`003`) | Beförderung: the member's own assessments and rank standings, me-scoped by construction, two reads that fail independently, and the three judgements the screen refuses to make on the organisation's behalf — including the standing deviation that this area has no design chapter |
| [`materialboerse.md`](materialboerse.md) (`REQ-APP-MARKET-*`) — **exists** (`001`–`009`) | Materialbörse: a board that conveys interest and carries no place or handover, one model for two halves whose write paths stay apart, an item that is not a material and a unit that travels with the row, „Ich kann liefern" as a toggle that is never offered on your own entry, the supporter list as the server's answer rather than the app's inference, writes that update their row while only a create re-reads, a picked material that is an id and is dropped the moment the field is edited, and the two things this slice reads but does not create |
| [`refinery.md`](refinery.md) (`REQ-APP-REF-*`) — **exists** (`001`–`006`) | Raffinerie: the member's own orders only, „Abholbereit" derived from a clock the screen owns rather than read from a status the server does not have, the two live filters as one request split on the device and therefore showing no total, a booking derived entirely from the order and never sent empty, the three rooms one booking has to announce, and the two recorded deviations from chapter 11 — no extractor import, and no invented UEX estimate |
| `privacy.md` (`REQ-APP-PRIV-*`) | dependency/data-flow gate, § 25 TDDDG storage table, permissions |
| [`ui.md`](ui.md) (`REQ-APP-UI-*`) — **exists** (`001`–`005`) | cross-screen behaviour: pull-to-refresh reaching an empty screen, a bottom sheet keeping its actions out of the gesture bar, the retry countdown of chapter 14 and the forced-update gate that stands outside every other gate and fails open three ways — including the rule only a screen can honour, that it replaces a screen with nothing on it and never one the member is already reading. Still to come: KRT design tokens, adaptive layout rules, i18n + copy rules, Fan Kit compliance band (see `core/designsystem/fankit/`) — extracted from the binding design spec at `docs/design/android/` |
| `offline.md` (`REQ-APP-OFFLINE-*`) | read-cache scope, backup exclusion, wipe semantics |

Server-side requirements (API exposure, rate limits, monitoring) stay in the main `basetool`
repo's `docs/specs/` — never duplicated here.
